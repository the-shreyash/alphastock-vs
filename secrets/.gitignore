# Everything in this directory is a credential — deny by default, then allow back
# the three files that are documentation and tooling. A deny-by-default rule is
# the only kind that stays correct when someone adds a new secret next month.
*
!.gitignore
!README.md
!generate.sh
