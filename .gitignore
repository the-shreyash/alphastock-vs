# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# IDE and editors
.idea/
.vscode/

# Dependencies
node_modules/
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Production builds
/build
dist/
dist

# Environment files (comprehensive coverage)

*token.json*
*credentials.json*

# Logs and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dump.rdb

# System files
.DS_Store
*.pem

# Python
__pycache__/
*pyc*
venv/
.venv/
# CI-managed virtualenv (.github/actions/setup-backend). Restored from the
# Actions cache on every run and rebuilt from the pinned requirements on a miss
# — never a source artifact, and never something a developer should commit.
.venv-ci/
.mypy_cache/

# CI test reports (JUnit XML). Produced by backend-ci.yml and uploaded as a
# build artifact; the run page is where they belong, not the repository.
test-results/

# Development tools
chainlit.md
.chainlit
.ipynb_checkpoints/
.ac

# Deployment
.vercel

# Data and databases
agenthub/agents/youtube/db

# Archive files and large assets
**/*.zip
**/*.tar.gz
**/*.tar
**/*.tgz
*.pack
*.deb
*.dylib

# Build caches
.cache/

memory/test_credentials.md

# Mobile development
android-sdk/
.env
.env.*
*.env
# …but committed example templates carry NO real secrets and MUST be shared so
# a new developer knows the full configuration surface (kept in sync with
# backend/security/secrets.py via scripts/generate_env_example.py).
!.env.example
!**/.env.example
credentials.json
*.key
.credentials

# Backup artifacts (PH2.9). The default BACKUP_ROOT is <repo>/backups, which is
# a developer-laptop convenience — a production BACKUP_ROOT belongs on a
# different filesystem entirely (docs/operations/BACKUP_AND_RESTORE.md §4).
# These files are a full copy of the database and of the deployment's secret
# material; committing one would put every user record and every signing key
# into git history permanently. Ignored by directory AND by artifact pattern, so
# a backup written anywhere in the tree still cannot be committed by accident.
backups/
*.archive.gz
*.archive.gz.enc
*.manifest.json
*.rejected
frontend/node_modules/.cache/default-development/5.pack
frontend/node_modules/.cache/default-development/8.pack
frontend/node_modules/.cache/default-development/9.pack
frontend/node_modules/.cache/default-development/12.pack
frontend/node_modules/.cache/default-development/13.pack

# Agent environment metadata
.emergent/
