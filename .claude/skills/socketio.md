# Socket.IO Skill

## Rules

Only one socket connection per user.

Never create multiple connections.

Use namespaces when appropriate.

Reconnect automatically.

Implement heartbeat.

Handle disconnect gracefully.

Always clean listeners on component unmount.

Socket events should be strongly typed.

Never emit unnecessary events.
